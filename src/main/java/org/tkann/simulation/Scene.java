package simulation;

/**
 * scene used for scene builder and RSSI to determine how the scene plays out and
 * from which dataset the RSSI is taken from (e.g. outside, inside, ...)
 */
public enum Scene {
    //TODO: complete class

}
